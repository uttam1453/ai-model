{
  "dependencies": {
    "aws-sdk": ">= 2.0.9",
    "axios": "^1.3.4",
    "buffer": "^6.0.3",
    "cors": "^2.8.5",
    "multer": "^1.4.2",
    "node-uuid": "^1.4.8",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "express": "^4.17.1",
    "fs-extra": "^2.1.2"
  },
  "scripts": {
    "start": "nodemon index.js ",
    "buildWeb": "cd web && npm run build && cp -r build ../. && cd .."
  }
}
